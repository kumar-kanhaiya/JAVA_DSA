package BasicsMath;

public class modulos {
    public static void main(String[] args) {
        /* properties of modulos
        (a+b)%m  = ((a % m) + (b % m)) % m
        (a - b) % m  = ((a % m) - (b % m)) % m
        (a * b)%m  = ((a % m) * (b % m)) % m
        (a / b)%m  = ((a % m) * (b inverse  % m)) % m
        b inverse means multiplicative modulos inverse
         */
    }
}
